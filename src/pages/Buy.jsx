import ListingPage from "./ListingPage";

export default function Buy() {
  return (
    <ListingPage
      type="buy"
      eyebrow="Buy a home"
      title="Properties for sale, chosen with care"
      description="Browse homes vetted by our team, with transparent pricing and virtual tours available on every listing."
    />
  );
}
